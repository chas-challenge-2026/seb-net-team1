// This environment only exists in DEBUG builds to test the csv parser.

#ifdef DEBUG
    #include <stdio.h>

    int main() {
        printf("=== libcsvparser Tester ===\n\n");
        printf("TODO: Invoke parse_csv when implemented to test functionality");
        return 0;
    }
#endif